libffi

github: https://github.com/libffi/libffi
license url: https://github.com/libffi/libffi/blob/master/LICENSE

The selected release is defined in `projects/versions.mk`.

On Android, executable memory must not be obtained with malloc; libffi uses the
platform-appropriate executable-memory path instead.
