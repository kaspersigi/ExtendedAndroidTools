diff --git a/src/ast/passes/ap_probe_expansion.cpp b/src/ast/passes/ap_probe_expansion.cpp
index a0a06d57..a380abac 100644
--- a/src/ast/passes/ap_probe_expansion.cpp
+++ b/src/ast/passes/ap_probe_expansion.cpp
@@ -365,9 +365,16 @@ void ProbeAndApExpander::visit(AttachPointList &aps)
         // available_filter_functions file or it doesn't exist on the system
         auto matches = probe_matcher_.get_matches_for_ap(*ap);
         if (matches.empty() && (pt != ProbeType::watchpoint)) {
-          if (is_kprobe && !bpftrace_.safe_mode_) {
-            new_aps.push_back(ap);
-            break;
+          if (is_kprobe) {
+            if (!bpftrace_.safe_mode_) {
+              new_aps.push_back(ap);
+              break;
+            }
+
+            if (func_info_.kernel_info().get_traceable_funcs().empty()) {
+              new_aps.push_back(ap);
+              break;
+            }
           }
 
           const auto missing_probes = bpftrace_.config_->missing_probes;
diff --git a/src/attached_probe.cpp b/src/attached_probe.cpp
index 455cf903..688342a1 100644
--- a/src/attached_probe.cpp
+++ b/src/attached_probe.cpp
@@ -24,6 +24,7 @@
 #include "bpftrace.h"
 #include "disasm.h"
 #include "log.h"
+#include "tracefs/tracefs.h"
 #include "symbols/kernel.h"
 #include "util/bpf_names.h"
 #include "util/cpus.h"
@@ -453,7 +454,19 @@ AttachedKprobeProbe::AttachedKprobeProbe(const Probe &probe,
 
 AttachedKprobeProbe::~AttachedKprobeProbe()
 {
-  if (bpf_link__destroy(link_)) {
+  if (probe_.attached_via_tracefs) {
+    for (int fd : probe_.perf_event_fds)
+      close(fd);
+
+    uint64_t offset = probe_.attach_point.empty() ? probe_.address
+                                                  : probe_.func_offset;
+    std::string ev_name = eventname(probe_, offset);
+    if (tracefs::detach_kprobe(ev_name))
+      LOG(WARNING) << "Failed to detach tracefs probe: " << ev_name;
+    return;
+  }
+
+  if (link_ && bpf_link__destroy(link_)) {
     LOG(WARNING) << "failed to destroy link for kprobe probe: "
                  << strerror(errno);
   }
@@ -461,6 +474,8 @@ AttachedKprobeProbe::~AttachedKprobeProbe()
 
 int AttachedKprobeProbe::link_fd()
 {
+  if (!link_)
+    return -1;
   return bpf_link__fd(link_);
 }
 
@@ -511,16 +526,70 @@ Result<std::unique_ptr<AttachedKprobeProbe>> AttachedKprobeProbe::make(
   auto *link = bpf_program__attach_kprobe_opts(prog.bpf_prog(),
                                                kprobe_func,
                                                &opts);
-  if (!link) {
-    if (errno == EILSEQ)
-      return make_error<AttachError>(
-          "Possible attachment attempt in the middle of an instruction, "
-          "try a different offset.");
-    return make_error<AttachError>();
+  if (link) {
+    return std::unique_ptr<AttachedKprobeProbe>(
+        new AttachedKprobeProbe(probe, link));
+  }
+
+  if (errno == EPERM || errno == ENOENT) {
+    std::string ev_name = eventname(probe, offset);
+    bool is_retprobe = probe.type == ProbeType::kretprobe;
+    std::string module = (!probe.path.empty() && probe.path != "vmlinux")
+                             ? probe.path
+                             : "";
+    int id = tracefs::create_tracefs_kprobe(
+        ev_name, is_retprobe, probe.attach_point, offset, module);
+    if (id < 0)
+      return make_error<AttachError>("tracefs fallback failed to create event");
+
+    struct perf_event_attr attr = {};
+    attr.type = PERF_TYPE_TRACEPOINT;
+    attr.size = sizeof(attr);
+    attr.config = id;
+    attr.sample_period = 1;
+    attr.sample_type = PERF_SAMPLE_RAW;
+    attr.wakeup_events = 1;
+
+    std::vector<int> fds;
+    for (int cpu : util::get_online_cpus()) {
+      int pfd = syscall(__NR_perf_event_open, &attr, -1, cpu, -1, 0);
+      if (pfd < 0) {
+        for (int fd : fds)
+          close(fd);
+        tracefs::detach_kprobe(ev_name);
+        return make_error<AttachError>();
+      }
+
+      if (ioctl(pfd, PERF_EVENT_IOC_SET_BPF, prog.fd()) < 0) {
+        close(pfd);
+        for (int fd : fds)
+          close(fd);
+        tracefs::detach_kprobe(ev_name);
+        return make_error<AttachError>();
+      }
+
+      if (ioctl(pfd, PERF_EVENT_IOC_ENABLE, 0) < 0) {
+        close(pfd);
+        for (int fd : fds)
+          close(fd);
+        tracefs::detach_kprobe(ev_name);
+        return make_error<AttachError>();
+      }
+
+      fds.push_back(pfd);
+    }
+
+    probe.perf_event_fds = std::move(fds);
+    probe.attached_via_tracefs = true;
+    return std::unique_ptr<AttachedKprobeProbe>(
+        new AttachedKprobeProbe(probe, nullptr));
   }
 
-  return std::unique_ptr<AttachedKprobeProbe>(
-      new AttachedKprobeProbe(probe, link));
+  if (errno == EILSEQ)
+    return make_error<AttachError>(
+        "Possible attachment attempt in the middle of an instruction, "
+        "try a different offset.");
+  return make_error<AttachError>();
 }
 
 class AttachedMultiKprobeProbe : public AttachedProbe {
diff --git a/src/probe_types.h b/src/probe_types.h
index 87a6d2ce..0ba8f9a2 100644
--- a/src/probe_types.h
+++ b/src/probe_types.h
@@ -121,6 +121,8 @@ struct Probe {
   uint64_t bpf_prog_id = 0;
   std::set<std::string> funcs;
   bool is_session = false;
+  bool attached_via_tracefs = false;
+  std::vector<int> perf_event_fds;
 
 private:
   friend class cereal::access;
diff --git a/src/tracefs/tracefs.cpp b/src/tracefs/tracefs.cpp
index 75dda379..a399b8f2 100644
--- a/src/tracefs/tracefs.cpp
+++ b/src/tracefs/tracefs.cpp
@@ -1,5 +1,10 @@
+#include <cerrno>
+#include <cstring>
+#include <fstream>
+#include <sstream>
 #include <unistd.h>
 
+#include "log.h"
 #include "tracefs.h"
 
 namespace bpftrace::tracefs {
@@ -24,4 +29,96 @@ std::string event_format_file(const std::string &category,
   return path("events/" + category + "/" + event + "/format");
 }
 
+constexpr const char *kprobe_events = "kprobe_events";
+
+static int write_kprobe(const std::string &ev_name,
+                        bool is_retprobe,
+                        const std::string &func,
+                        uint64_t offset,
+                        const std::string &module)
+{
+  std::string kprobe_event_file = path(kprobe_events);
+  std::ofstream kprobe_events_stream(kprobe_event_file, std::ios_base::app);
+  if (kprobe_events_stream.fail()) {
+    LOG(ERROR) << "Failed to open " << kprobe_event_file << ": "
+               << strerror(errno);
+    return -1;
+  }
+
+  std::stringstream kprobe_def;
+  kprobe_def << (is_retprobe ? "r" : "p") << ":" << ev_name << " ";
+  if (!module.empty())
+    kprobe_def << module << ":";
+
+  if (func.empty()) {
+    kprobe_def << "0x" << std::hex << offset;
+  } else {
+    kprobe_def << func << "+" << offset;
+  }
+
+  LOG(V1) << "Writing " << kprobe_def.str() << " to " << kprobe_events;
+  kprobe_events_stream << kprobe_def.str() << std::endl;
+  if (kprobe_events_stream.fail()) {
+    LOG(ERROR) << "Failed to write to " << kprobe_events << ": "
+               << strerror(errno);
+    return -1;
+  }
+
+  return 0;
+}
+
+int create_tracefs_kprobe(const std::string &ev_name,
+                          bool is_retprobe,
+                          const std::string &func,
+                          uint64_t offset,
+                          const std::string &module)
+{
+  if (write_kprobe(ev_name, is_retprobe, func, offset, module))
+    return -1;
+
+  std::string event_path = path("events/kprobes/" + ev_name);
+  std::string id_path = event_path + "/id";
+
+  std::ifstream id_file(id_path);
+  if (id_file.fail()) {
+    LOG(ERROR) << "Could not open tracefs event ID file '" << id_path
+               << "': " << strerror(errno);
+    detach_kprobe(ev_name);
+    return -1;
+  }
+
+  int id;
+  id_file >> id;
+  if (id_file.fail()) {
+    LOG(ERROR) << "Could not read from tracefs event ID file '" << id_path
+               << "': " << strerror(errno);
+    detach_kprobe(ev_name);
+    return -1;
+  }
+
+  return id;
+}
+
+int detach_kprobe(const std::string &ev_name)
+{
+  std::string kprobe_event_file = path(kprobe_events);
+  std::ofstream kprobe_events_stream(kprobe_event_file, std::ios_base::app);
+  if (kprobe_events_stream.fail()) {
+    LOG(ERROR) << "Failed to open " << kprobe_event_file << ": "
+               << strerror(errno);
+    return -1;
+  }
+
+  std::string kprobe_def = "-:" + ev_name;
+  LOG(V1) << "Writing " << kprobe_def << " to " << kprobe_events;
+  kprobe_events_stream << kprobe_def << std::endl;
+  if (kprobe_events_stream.fail()) {
+    LOG(ERROR) << "Failed to write to " << kprobe_events << ": "
+               << strerror(errno);
+    return -1;
+  }
+
+  return 0;
+}
+
 } // namespace bpftrace::tracefs
diff --git a/src/tracefs/tracefs.h b/src/tracefs/tracefs.h
index cd832080..78790fc5 100644
--- a/src/tracefs/tracefs.h
+++ b/src/tracefs/tracefs.h
@@ -1,5 +1,6 @@
 #pragma once
 
+#include <cstdint>
 #include <string>
 
 namespace bpftrace::tracefs {
@@ -26,4 +27,12 @@ inline std::string available_filter_functions()
 std::string event_format_file(const std::string &category,
                               const std::string &event);
 
+int create_tracefs_kprobe(const std::string &ev_name,
+                          bool is_retprobe,
+                          const std::string &func,
+                          uint64_t offset,
+                          const std::string &module);
+
+int detach_kprobe(const std::string &ev_name);
+
 } // namespace bpftrace::tracefs
